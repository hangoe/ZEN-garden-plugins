Plugin Template
====================

Overview
--------

``plugin.py`` shows the most simple implementation of a plugin. It implements one function that
subscribes to the event ``test_event1``. It prints to the console when the event is triggered.

Example
--------

.. literalinclude:: ../../../plugin_template/plugin.py
   :language: python

